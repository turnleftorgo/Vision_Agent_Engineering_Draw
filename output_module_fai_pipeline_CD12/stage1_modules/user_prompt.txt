找出这张图像中的所有独立内容模块，并返回每个完整模块的边界框。每个边界框必须同时包含模块主体及所有明确属于它的周边参数标注，且不得截断这些内容；不要包含相邻模块的无关内容。

输出格式：
{
  "objects": [
    {
      "id": 1,
      "bbox": [x_min, y_min, x_max, y_max],
      "description": "该区域内容的简短描述"
    }
  ]
}